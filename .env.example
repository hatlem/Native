# Copy to .env and adjust. Postgres connection for Prisma.
# Local docker-compose default:
DATABASE_URL="postgresql://benative:benative@127.0.0.1:5432/benative?schema=public"

# App
NEXT_PUBLIC_APP_NAME="BeNative"

# Auth.js (NextAuth v5). Generate with: openssl rand -base64 32
AUTH_SECRET="change-me"

# Optional seed overrides for the desk admin account
# DESK_ADMIN_EMAIL="desk@benative.example"
# DESK_ADMIN_PASSWORD="benative-desk"

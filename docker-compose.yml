services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: benative
      POSTGRES_PASSWORD: benative
      POSTGRES_DB: benative
    ports:
      - "5432:5432"
    volumes:
      - benative_pgdata:/var/lib/postgresql/data

volumes:
  benative_pgdata:

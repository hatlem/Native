services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: atnative
      POSTGRES_PASSWORD: atnative
      POSTGRES_DB: atnative
    ports:
      - "5432:5432"
    volumes:
      - atnative_pgdata:/var/lib/postgresql/data

volumes:
  atnative_pgdata:
